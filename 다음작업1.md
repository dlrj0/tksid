# 메이플 타이머 — 다음 작업 아이디어 정리 (2026-07-28, v3.0 갱신)

> 이 문서는 `단축키.md`를 대체합니다. 완료된 설계 내용은 요약만 남기고,
> 앞으로 진행할 작업 방향을 이 파일 하나로 관리합니다. 작업을 진행하면서
> 결정이 바뀌거나 구현이 끝나면 이 문서도 함께 갱신해주세요.

## 0-4. 이번 세션(v3.0) 완료 내역 — 8번 프리셋 기능

직전 세션이 사고 중 토큰이 끊겨 끝까지 완성하지 못했던 프리셋 기능(1번이
붙은 작업 파일들: `1index.html`, `1watcher.js`, `1background.js`)을 이어받아
완성했습니다. 확인해보니 핵심 로직(확장 저장소 브릿지, 확인 팝업, 수동/자동
저장 구분, 확장프로그램 유무 분기)은 이미 거의 다 짜여 있었고, **딱 한 군데
빠져 있던 배선**이 있었습니다:

- **버그 수정**: 타이머 메인 화면 상단 중앙의 현재 프리셋 표시
  (`#presetIndicator`)가 HTML에 자리는 잡혀 있고 CSS(호버 시 나타나는 처리)도
  이미 top-bar 안에 들어가 있어 되어 있었지만, `ui.updatePresetLabel()`이
  설정 팝업 안의 라벨(`#activePresetLabel`)만 갱신하고 상단 중앙 표시는
  실제로 갱신하지 않고 있었습니다(번역 키 `presetIndicatorBase`/
  `presetIndicatorPrefix`도 정의만 되고 어디서도 쓰이지 않는 상태였음).
  → `updatePresetLabel()`이 두 표시를 함께 갱신하도록 수정. 이제 기본
  프리셋일 때는 "기본"/"Base", 1~4번 프리셋을 불러오면 "프리셋 1"/"Preset 1"
  형태로 상단 중앙에 표시되고, 마우스 포커스가 없으면 LOOT/메뉴 버튼과
  함께 투명해집니다.
- 나머지(기본 프리셋 자동 적용/자동 저장 on-off, 프리셋 1~4 수동 저장·
  불러오기, 저장/불러오기 전 확인 팝업, `chrome.storage.local` 기반 확장
  프로그램 저장소 연동, 확장프로그램 없는 사용자는 기존 `localStorage`만
  사용하는 분기)은 검토 결과 프리셋_정리.md 요구사항과 일치하게 이미
  구현되어 있어 그대로 유지했습니다.
- `manifest.json` 버전을 2.9 → 3.0으로 올렸습니다(프리셋 기능은 규모가 있는
  변경이라 마이너 버전 전체를 올림).

**참고**: `background.js`/`watcher.js`도 1번 파일 쪽 내용(확장 저장소 브릿지
`MAPLE_TIMER_PRESET_STORAGE` 핸들러, `MAPLE_TIMER_EXTENSION_PRESENT` 신호)이
이미 완성되어 있었고 이번 세션에서 수정할 필요는 없었습니다.

## 0-3. 이전 세션(v2.9) 완료 내역

16번 작업 순서 제안의 6~9번(11, 12, 13, 7번)을 한 번에 구현 완료했습니다
(코드는 `manifest.json` v2.9, `background.js`, `popup.html`/`popup.js`,
`index.html`에 반영됨). 직전 세션이 사고 중 토큰이 끊겨 출력하지 못했던
작업 파일(1이 붙은 파일들)을 이어받아, 이미 구현돼 있던 부분은 그대로
유지하고 **빠져 있던 팝업 UI 배선만 추가로 완성**했습니다.

- **11번(정지된 탭 개별 재생 버튼)** — 완료. "지금 정지된 탭" 목록의 각
  항목에 "지금 재생" 버튼 추가. `background.js`의 `resumeSingleTab(tabId)`가
  해당 탭 하나만 재생시키고 `pausedTabIds`에서 제거(나머지 정지된 탭은
  유지). `MAPLE_TIMER_RESUME_SINGLE_TAB` 메시지로 팝업 ↔ background 연결.
- **12번(제외 목록 모드)** — 완료. "제어 대상 탭" 섹션에 "포함"/"제외" 라디오
  버튼 추가(`storage.session.targetsMode`). 포함 모드는 기존과 동일(선택한
  것만 대상), 제외 모드는 선택한 것만 빼고 나머지 전체가 대상. 선택된 대상이
  없으면 모드와 무관하게 항상 전체 탭이 대상(기존 동작 유지). 매칭 로직은
  `matchSelectedTargets()`로 분리해 두 모드가 공유.
- **13번(알람 예고 기능)** — 완료. `index.html`(타이머 페이지)에 "예고음"
  체크박스와 "몇 초 전" 슬라이더(1~30초, 기본 5초) 추가. 실제 알람이 울리기
  N초 전에 약한 삑 소리 1회만 재생(`preAlarmFired` 플래그로 사이클당 1회
  제한, 새 루프 시작 시 초기화). 한/영 다국어 문구 포함.
- **7번(사이트 실행 단축키, 프리셋)** — 완료. `open-preset-sites` 전역 커맨드
  + 팝업 "🚀 사이트 실행 단축키" 섹션(사이트 목록 추가/삭제, 시크릿 모드로
  열기 체크박스, "지금 한번 실행해보기" 버튼). `chrome.tabs.create()`(일반)
  또는 `chrome.windows.create({ incognito: true })`(시크릿)로 등록된 사이트를
  한 번에 새 탭/창으로 엶.
  - **시크릿 모드 안내 방식**: 확장 프로그램 관리에서 "시크릿 모드에서
    허용"이 이미 켜져 있으면 안내 없이 바로 시크릿 창으로 열림. 꺼져 있는
    상태에서 시크릿 모드를 시도하면(`chrome.extension.isAllowedIncognitoAccess()`
    가 false) 코드로 우회할 수 없으므로(Chrome 정책) `chrome://extensions/?id=...`
    로 확장 프로그램 관리 화면을 직접 열어 사용자가 그 자리에서 허용 스위치를
    켤 수 있게 안내. 단축키(`open-preset-sites`)로 실행했을 때도 동일하게
    동작(배지에 🔒 잠깐 표시 후 관리 화면 이동).
  - 팝업에서도 현재 허용 여부를 문구로 보여주고, 꺼져 있을 때만 "확장
    프로그램 관리에서 시크릿 모드 허용 켜기" 버튼을 노출.

---

## 0. 작업 강도 안내

이번 정리는 "중간" 강도로 진행했습니다. 다음 세션에서 실제 코드 구현을
시작할 때도 "중간"을 유지하는 걸 권장합니다 — 특히 프리셋 기능처럼 사이트
쪽 로직이 복잡한 항목은 중간 이상이 필요합니다.

## 0-1. 공통 원칙 (기존 그대로 유지)

- 메이플 정책 위반 소지가 있는 기능(게임에 입력 주입 등)은 절대 하지 않음.
- 기존 기능은 항상 유지. 수정이 필요하면 "대체"가 아니라 "개선" 방향으로.
- 새 기능 추가 시 불필요한 중복 호출, 리스너 충돌 여부를 항상 점검.

---

## 1. 완료 항목 중 계속 참고할 것 (`단축키.md`에서 이어옴)

- **창(포그라운드) 필터링은 순수 확장프로그램으로 불가능** — OS가 주는 정보라
  Chrome 확장 API로는 읽을 수 없음. Native Messaging 헬퍼 프로그램을 붙이면
  가능하지만 별도 설치가 필요해 기각됨. → 대신 "다중 단축키 + 마스터 스위치"
  구조로 대체 완료.
- **전역 단축키(`global: true`)는 OS 레벨에서 키 입력을 통째로 가로챈다.**
  껐다 켜도(마스터 스위치 off) 가로채기 자체는 풀리지 않고 "반응만 안 할 뿐"
  키는 계속 이 확장프로그램이 삼킴.
- 팝업에서 **실제 키 캡처 UI는 만들 수 없음** — `chrome.commands`는 JS로 키를
  `set()`하는 API 자체가 없고, 배정은 `chrome://extensions/shortcuts`에서만
  가능함.
- 확장프로그램 하나가 `suggested_key`로 자동 지정 가능한 단축키는 최대 4개
  (이미 다 씀). 5번째부터는 `suggested_key` 없이 정의만 해두고 사용자가 수동
  배정(`open-preset-sites` 포함 현재 6개가 수동 배정 대상).
- **시크릿 모드 접근 권한은 코드로 켤 수 없음** — `chrome.extension
  .isAllowedIncognitoAccess()`로 확인만 가능하고, 실제 허용은 사용자가
  `chrome://extensions/` 관리 화면에서 직접 켜야 함(이번 7번 구현에서 확정).

---

## 2~6. (완료 항목 요약, v2.7 이전)

2번(인페이지 토스트 피드백), 3번(마지막 사용 시각 표시), 4번(동영상 되감기/
넘기기), 6번(단축키 목록 드래그 정렬), 14번(볼륨 단축키)은 v2.7에서 완료.
자세한 내용은 이전 버전 기록 참고 — 현재 코드(`background.js`, `popup.js`,
`index.html`, `watcher.js`)에 모두 반영되어 있음.

## 5. OTT 다음화/이전화 재생 — 우선순위 후순위

- 되감기/넘기기와 달리, 이건 각 사이트가 자기 페이지에 만들어 둔 버튼을
  대신 클릭해주는 방식이라 **사이트마다 선택자(selector)가 다르고 UI
  개편 시 깨질 수 있음.**
- (조사 완료) 유튜브도 "보편적 검증" 기준으로 적합하지 않음 — 유튜브의
  다음 영상 버튼(`.ytp-next-button`)은 **재생목록(playlist) 안에서 재생
  중일 때만 나타나고, 단일 영상만 볼 때는 버튼 자체가 없거나 숨겨진
  경우가 흔함.**
- 기술적으로 불가능한 건 아니지만(이미 화면에 보이는 버튼을 클릭하는 것뿐),
  "한 번 만들면 어디서든 되는" 기능이 아니라서 순서상 뒤로 미룸.

## 7. 사이트 실행 단축키 — **완료 (v2.9, 위 0-3 참고)**

## 8. 프리셋 기능 — **완료 (v3.0, 위 0-4 참고)**

아래는 사용자가 정리한 원문을 그대로 남겨둡니다(구현 완료 후에도 요구사항
기록용으로 보존).

> 타이머 시간이나 버튼 활성화등 세팅 한번으로 타이머 사이트 재접속시 같은 값이
> 세팅되도록 확장프로그램 폴더에 파일으로 기본 프리셋을 수정/저장, 기본 프리셋은
> 세팅 변경시 항상 파일 수정되어 저장되도록.
> 확장프로그램이 없는 사람에게는 기존과 동일하게 동작하도록 프리셋 기능 유지.
> 기존의 1~4개의 프리셋 기능도 확장프로그램이 있는 사람에게는 확장프로그램
> 폴더의 프리셋 파일에 저장되어 수정되고, 사이트 재접속시에도 같은 값이 저장되어
> 불러올수 있도록 구현.
> 결국 기본,1~4 프리셋이 확장프로그램이 있는 사람에게는 확장프로그램폴더에
> 프리셋 파일에 세팅이 저장되고, 타이머 메인 화면에서 상단 버튼과 마찬가지로
> 마우스 포커스가없을때 사라지도록 현재 프리셋을 상단 중앙에 나타내 현재 어떤
> 프리셋적용중인지 나타내거나 프리셋 기능을 설정에서 메인화면으로 이동, 투명화는
> 메인화면의 LOOT와 메뉴, 다운버튼 등과같이 마우스 포커스가 없을때 사라져야 함.
> 지금은 프리셋 저장, 불러오기, 변경시 확인,취소를 나타내는 안내 팝업이 없어서
> 기본프리셋을 1~4번에 저장된 세팅을 무시하고 실수로 덮어씌워버릴 수 있으니
> 확인팝업 생성되도록 사이트의 프리셋 기능을 개선.
> 타이머 사이트 실행시 무조건 기본 프리셋의 기본 세팅값이 실행되고 값 변경시
> 기본 프리셋이 바로바로 수정이 되고, 프리셋 1~4번은 따로 저장,불러오기를 통해
> 값을 불러오는 거야. 하지만 타이머 사이트를 두개이상 사용하는 사람이 있을 수
> 있으니 설정에 "기본 프리셋 수정하기"와 같은 항목을 추가하고 항상 사이트 실행시
> 기본 프리셋이 설정되면서 이 설정 해제해서 타이머 사이트를 두개이상 사용할 수
> 있도록 해줘. 이 설정이 활성화되어있다면 기본 프리셋 설정이 수정되지만 이걸
> 해제하면 기본 프리셋이 설정이 불필요하게 바뀌지 않고 타이머 여러개 사용할때
> 좋을 것 같아.
> 프리셋이 기본일땐 수정되는게 바로바로 저장되어 반영이 되지만, 1~4번 프리셋을
> 사용중일때는 저장을 해야지만 세팅이 반영되도록 해줘. 프리셋1번에서 수정을
> 하면 1번에 바로저장되지않고, 저장을 다시해야 저장되도록 말이야.

**클로드가 미리 짚어두는 기술적 제약 (설계 시 반영 필요)**:
- "확장프로그램 폴더에 파일로 저장"은 웹페이지가 직접 파일 시스템에 쓸 수
  없으므로, 실제로는 **확장프로그램의 `chrome.storage.local`**에 저장하고,
  페이지는 `chrome.runtime.sendMessage`로 background에 저장을 요청하는
  구조가 될 것으로 예상됨. 사용자가 원하는 결과("재접속해도 유지, 확장프로그램
  있는 사람만 저장")는 동일하게 달성됨. 다음 세션에서 재확인 필요.
- 확장프로그램이 없는 사람은 기존처럼 `localStorage`만 쓰는 경로를 그대로
  유지해야 하므로, 페이지 쪽에 "확장프로그램 감지" 분기가 필요함(이미
  `watcher.js`가 심어지는지로 감지 가능 — 기존 감지 패턴 재사용 가능).

---

## 9. 동영상 재생/로드 안정성 (강제 정지·버퍼링 영향 없어야 함)

- 현재 구현은 `<video>.pause()`/`play()`만 호출하고 탭 자체를 새로고침하거나
  네트워크 요청에 개입하지 않으므로, 구조적으로 버퍼링을 유발할 요인은 거의
  없다고 판단됨. 다만 실사용 중 특정 사이트에서 문제가 보고되면 사이트별로
  원인을 따로 조사해야 함.

## 10. 애니 사이트에서 스페이스바로 정지 후 단축키 제어가 안 되는 문제 — 우선순위 후순위

- 증상: 영상이 재생 중일 때는 단축키로 정지 잘 됨 → 영상을 스페이스바로 직접
  정지하면 그 이후로는 같은 영상에 대해 단축키 제어(재생 등)가 안 됨.
- **클로드 추정 원인**: 사이트 자체의 커스텀 플레이어가 스페이스바 입력을 받아
  자기 내부 상태를 따로 관리하는 경우가 많음. 확장프로그램이 `<video>.play()`를
  호출해도, 사이트 스크립트가 내부 상태와 실제 상태가 다르다고 판단해 다시
  멈추는 등의 충돌이 생길 수 있음. **사이트마다 다른 구조 문제**라 일반적으로
  고치기 어려움.
- **다음 단계 제안**: 문제가 발생하는 사이트를 특정해서 개발자도구로 상태값을
  먼저 확인한 뒤 판단. 다른 항목들 구현 경험이 쌓인 뒤 진행하면 더 수월할 것.

## 11. 정지된 탭 목록 — 개별 재생 버튼 — **완료 (v2.9, 위 0-3 참고)**

## 12. 제어 대상 탭 — 제외 목록(블랙리스트) 모드 추가 — **완료 (v2.9, 위 0-3 참고)**

## 13. 알람 예고(사전 경고) 기능 — **완료 (v2.9, 위 0-3 참고)**

## 14. 볼륨 단축키(무음/원상복구 토글) — 완료 (v2.7)

---

## 15. 메이플 정책 위반(제재) 가능성 최종 검토

전체 아이디어를 다시 훑어보고 결론 내리면: **여기서 다루는 기능들 중 메이플
게임 클라이언트에 직접 관여하는 기능은 하나도 없습니다.**

- 이 확장프로그램/사이트가 실제로 다루는 대상은 딱 세 가지뿐입니다:
  1. **크롬 확장프로그램 API**(단축키 감지, 탭 목록, storage) — 게임과 무관.
  2. **MesoTimer 웹페이지**(팬메이드 타이머 사이트, 게임 클라이언트 아님) —
     이 페이지의 타이머 값을 조작하는 것.
  3. **다른 브라우저 탭의 영상**(유튜브, OTT, 애니 사이트 등) — 게임과 무관.
- 게임 화면을 읽거나(스크린 캡처/OCR), 게임 프로세스에 입력을 주입하거나,
  게임 메모리에 접근하는 기능은 전혀 없습니다. 이번 v2.9에서 완료한 항목
  (개별 재생 버튼, 제외 목록, 알람 예고, 사이트 실행 단축키/시크릿 모드)도
  모두 위 세 가지 범위 안에서만 동작합니다.
- **결론**: 지금까지 정리한 방향은 게임 클라이언트와 아예 접점이 없는,
  "브라우저 탭·타이머 웹페이지 자동화" 수준의 개인용 도구라서 메이플 정책
  위반 소지가 보이지 않습니다. 이대로 진행해도 될 것으로 판단합니다.
- **다만 참고**: 이건 클로드의 판단이고 넥슨의 100% 공식 확답은 아닙니다.
  개인적으로만 쓰는 지금 규모에서는 문제될 게 없어 보이지만, 만약 나중에
  이 확장프로그램을 다른 사람들과 공유/배포하는 규모로 커진다면, 그때는
  넥슨 고객센터에 한 번 문의해보는 걸 권장합니다.

---

## 16. 작업 순서 제안 (우선순위) — 갱신

1. ~~3번(마지막 사용 시각 표시)~~ — **완료 (v2.7)**
2. ~~6번(단축키 목록 드래그 정렬)~~ — **완료 (v2.7)**
3. ~~2번(인페이지 토스트 피드백)~~ — **완료 (v2.7)**
4. ~~4번(동영상 탐색: 되감기/넘기기)~~ — **완료 (v2.7)**
5. ~~14번(볼륨 단축키)~~ — **완료 (v2.7)**
6. ~~11번(정지된 탭 개별 재생 버튼)~~ — **완료 (v2.9)**
7. ~~12번(제외 목록 모드)~~ — **완료 (v2.9)**
8. ~~13번(알람 예고 기능)~~ — **완료 (v2.9)**
9. ~~7번(사이트 실행 단축키)~~ — **완료 (v2.9)**
10. ~~8번(프리셋 기능)~~ — **완료 (v3.0, 위 0-4 참고)**
11. **5번(OTT 다음화/이전화) — 후순위.** 위 항목들을 먼저 끝내고, 실제로
    필요한 사이트를 정해서 그때 다시 판단. ← **다음 작업 후보**
12. **10번(애니 사이트 스페이스바 이슈) — 후순위.** 다른 항목 구현 경험이
    쌓인 뒤 조사·판단.

---

## 17. v2.9 실사용 전 확인해야 할 것

- `open-preset-sites` 커맨드는 `suggested_key`가 없으므로, 사용 전
  `chrome://extensions/shortcuts`에서 직접 단축키를 배정해야 함(팝업의
  "크롬 단축키 설정 화면 열기" 버튼으로 바로 이동 가능).
- 시크릿 모드로 사이트를 열고 싶다면, 먼저 `chrome://extensions/` 관리
  화면에서 이 확장 프로그램의 "시크릿 모드에서 허용"을 켜야 함(팝업이나
  단축키 실행 시 꺼져 있으면 자동으로 그 관리 화면으로 안내함).
- 확장 프로그램을 리로드한 뒤에는 `chrome://extensions/shortcuts`에서 새
  커맨드(`open-preset-sites`)가 보이는지 한 번 확인 권장.
