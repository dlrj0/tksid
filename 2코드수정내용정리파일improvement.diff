--- a/index.html
+++ b/index.html
@@ -563,6 +563,28 @@
             }
         };
 
+        // [RESET-SIGNAL] Mirrors the setAlarmClass() pattern above: the extension's
+        // content script (watcher.js) can only be injected into the real tab, never
+        // into a PiP window, so any DOM signal it needs to observe must always be
+        // reflected onto BOTH the currently visible body and the original
+        // document.body. Unlike "alarm-state" (an on/off class), a reset is a
+        // one-shot event, so we use a monotonically increasing counter attribute
+        // instead — watcher.js just needs to see the value change.
+        // `readyForResetSignals` stays false until the very first core.reset() call
+        // on page load finishes, so that initial setup never fires a spurious
+        // "play" signal to other tabs.
+        let readyForResetSignals = false;
+        let resetSeq = 0;
+        const notifyReset = () => {
+            if (!readyForResetSignals) return;
+            resetSeq++;
+            const b = getBody();
+            if (b) b.setAttribute("data-reset-seq", String(resetSeq));
+            if (document.body && document.body !== b) {
+                document.body.setAttribute("data-reset-seq", String(resetSeq));
+            }
+        };
+
         const core = {
             start: () => {
                 audio.init(); 
@@ -582,7 +604,8 @@
                 
                 if(state.loop) b.classList.add("is-loop");
 
-                state.waiting = false; 
+                state.waiting = false;
+                notifyReset(); // [RESET-SIGNAL] covers first click-to-start, wheel-triggered restart, Space-key start
                 let tVal = parseFloat($('targetTime').value);
                 state.target = isNaN(tVal) || tVal < 0 ? 100 : tVal;
                 if($('targetTime').value != state.target) $('targetTime').value = state.target;
@@ -655,6 +678,7 @@
                 startTimeRef = 0;
 
                 state.running = false; state.paused = false; state.waiting = false;
+                notifyReset(); // [RESET-SIGNAL] covers dblclick reset, KeyR, btnMode, loadPreset — but NOT the very first page-load reset (guarded by readyForResetSignals)
                 
                 getBody().classList.remove("is-paused", "is-loop");
                 setAlarmClass(false);
@@ -709,6 +733,7 @@
                             if(state.syncLoop) audio.stop(); 
                             state.waiting = false; 
                             setAlarmClass(false);
+                            notifyReset(); // [RESET-SIGNAL] automatic loop-restart after alarmDuration elapses
                             ui.updateFavicon('active'); 
 
                             state.duration = state.target;
@@ -1135,6 +1160,7 @@
         setupEvents(document);
         ui.sync();
         core.reset();
+        readyForResetSignals = true; // [RESET-SIGNAL] initial page-load reset above must NOT trigger a play signal
     </script>
 </body>
 </html>
\ No newline at end of file
--- a/watcher.js
+++ b/watcher.js
@@ -7,23 +7,33 @@
   if (!isTimerPage) return; // 타이머 페이지가 아니면 아무 것도 하지 않음
 
   let wasAlarming = false;
+  // [RESET-SIGNAL] index.html의 notifyReset()이 body에 찍는, 1씩 증가하는 카운터.
+  // 값이 바뀔 때마다(리셋이 발생할 때마다) 재생 신호를 보냄.
+  let lastResetSeq = document.body ? document.body.getAttribute("data-reset-seq") : null;
 
   function checkAlarmState() {
     const body = document.body;
     if (!body) return;
     const isAlarming = body.classList.contains("alarm-state");
 
-    // 알람이 "꺼짐 -> 켜짐"으로 바뀌는 순간에만 신호를 보냄
+    // 알람이 "꺼짐 -> 켜짐"으로 바뀌는 순간에만 신호를 보냄 (정지 신호)
     if (isAlarming && !wasAlarming) {
       chrome.runtime.sendMessage({ type: "MAPLE_TIMER_ALARM" });
     }
     wasAlarming = isAlarming;
+
+    // [RESET-SIGNAL] 리셋 카운터가 바뀌면 재생 신호를 보냄
+    const currentResetSeq = body.getAttribute("data-reset-seq");
+    if (currentResetSeq !== null && currentResetSeq !== lastResetSeq) {
+      lastResetSeq = currentResetSeq;
+      chrome.runtime.sendMessage({ type: "MAPLE_TIMER_RESET" });
+    }
   }
 
   const observer = new MutationObserver(checkAlarmState);
   observer.observe(document.body, {
     attributes: true,
-    attributeFilter: ["class"],
+    attributeFilter: ["class", "data-reset-seq"],
   });
 
   // 초기 상태 체크(새로고침 직후 이미 알람 상태일 수도 있으므로)
--- a/background.js
+++ b/background.js
@@ -1,40 +1,157 @@
 // background.js
 
+// 각 탭 안에서 실행되는 함수들 -----------------------------------------
+
 function pauseAllMediaInPage() {
-  // 이 함수는 각 탭 안에서 실행됩니다.
+  // 반환값: 이 페이지에서 실제로 하나라도 정지시켰는지 여부.
+  // (확장 프로그램이 "직접 정지시킨 탭"만 나중에 재생시키기 위한 추적용)
+  let pausedAny = false;
   document.querySelectorAll("video, audio").forEach((m) => {
     if (!m.paused) {
       try {
         m.pause();
+        pausedAny = true;
       } catch (e) {
         /* 무시 */
       }
     }
   });
+  return pausedAny;
 }
 
-chrome.runtime.onMessage.addListener((message, sender) => {
-  if (message?.type !== "MAPLE_TIMER_ALARM") return;
-
-  chrome.storage.local.get(["enabled"], ({ enabled }) => {
-    // 기본값은 켜짐(true)
-    if (enabled === false) return;
-
-    chrome.tabs.query({}, (tabs) => {
-      for (const tab of tabs) {
-        if (!tab.id) continue;
-        if (sender.tab && tab.id === sender.tab.id) continue; // 타이머 탭 자신은 제외
-        if (!/^https?:/.test(tab.url || "")) continue; // 확장 프로그램/설정 페이지 등은 제외
-
-        chrome.scripting
-          .executeScript({
-            target: { tabId: tab.id },
-            func: pauseAllMediaInPage,
-          })
-          .catch(() => {
-            /* 권한이 없는 특수 페이지 등은 조용히 무시 */
-          });
+function playAllMediaInPage() {
+  document.querySelectorAll("video, audio").forEach((m) => {
+    try {
+      const p = m.play();
+      if (p && typeof p.catch === "function") {
+        // 자동재생 정책 등으로 거부되어도 조용히 무시 (요구사항 3-4)
+        p.catch(() => {});
       }
-    });
+    } catch (e) {
+      /* 무시 */
+    }
   });
+}
+
+// 탭 선택/매칭 -----------------------------------------------------------
+
+function getDomain(url) {
+  try {
+    return new URL(url).hostname;
+  } catch (e) {
+    return "";
+  }
+}
+
+function isControllableTab(tab) {
+  return !!tab.id && /^https?:/.test(tab.url || "");
+}
+
+// 사용자가 선택한 대상(chrome.storage.session의 selectedTargets)을 실제 열려있는
+// 탭과 매칭한다. 선택된 대상이 하나도 없으면(기본값) 열려있는 모든 http(s) 탭을 반환.
+// (요구사항 B: 선택 안 하면 전체 대상 — 기존 동작 유지)
+async function resolveTargetTabs(excludeTabId) {
+  const allTabs = await chrome.tabs.query({});
+  const candidateTabs = allTabs.filter(
+    (t) => isControllableTab(t) && t.id !== excludeTabId
+  );
+
+  const { selectedTargets } = await chrome.storage.session.get([
+    "selectedTargets",
+  ]);
+
+  if (!selectedTargets || selectedTargets.length === 0) {
+    return candidateTabs; // 기본값: 전체 탭 대상
+  }
+
+  const resolved = new Map();
+  for (const target of selectedTargets) {
+    // 1순위: 저장해둔 tabId가 여전히 열려있으면 그대로 사용 (가장 정확함)
+    let tab = candidateTabs.find((t) => t.id === target.tabId);
+
+    // 2순위: 새로고침 등으로 tabId가 바뀌었을 경우, 지정한 매칭 기준으로 재탐색
+    // (요구사항 3-3: 탭마다 도메인/제목 중 사용자가 고른 기준으로 매칭)
+    if (!tab) {
+      if (target.matchBy === "title") {
+        tab = candidateTabs.find((t) => t.title === target.titleAtSelection);
+      } else {
+        tab = candidateTabs.find((t) => getDomain(t.url) === target.domain);
+      }
+    }
+
+    if (tab) resolved.set(tab.id, tab);
+  }
+  return Array.from(resolved.values());
+}
+
+// 메시지 처리 --------------------------------------------------------------
+
+chrome.runtime.onMessage.addListener((message, sender) => {
+  if (message?.type === "MAPLE_TIMER_ALARM") {
+    handleAlarm(sender);
+  } else if (message?.type === "MAPLE_TIMER_RESET") {
+    handleReset(sender);
+  }
+  // 비동기 응답이 필요 없으므로 별도 return 값 없음
 });
+
+async function handleAlarm(sender) {
+  const { enabled } = await chrome.storage.local.get(["enabled"]);
+  if (enabled === false) return; // 기본값은 켜짐(true)
+
+  const tabs = await resolveTargetTabs(sender?.tab?.id);
+  const newlyPausedIds = [];
+
+  for (const tab of tabs) {
+    try {
+      const results = await chrome.scripting.executeScript({
+        target: { tabId: tab.id },
+        func: pauseAllMediaInPage,
+      });
+      const didPause = results?.[0]?.result === true;
+      if (didPause) newlyPausedIds.push(tab.id);
+    } catch (e) {
+      /* 권한이 없는 특수 페이지 등은 조용히 무시 */
+    }
+  }
+
+  if (newlyPausedIds.length > 0) {
+    const { pausedTabIds = [] } = await chrome.storage.session.get([
+      "pausedTabIds",
+    ]);
+    const merged = Array.from(new Set([...pausedTabIds, ...newlyPausedIds]));
+    await chrome.storage.session.set({ pausedTabIds: merged });
+  }
+}
+
+async function handleReset(sender) {
+  const { enabled } = await chrome.storage.local.get(["enabled"]);
+  if (enabled === false) return;
+
+  // 요구사항 3-2: 확장 프로그램이 "직접 정지시킨 탭"에만 재생 신호를 보냄.
+  // 사용자가 스스로 일시정지한 영상까지 강제로 재생시키지 않기 위함.
+  const { pausedTabIds = [] } = await chrome.storage.session.get([
+    "pausedTabIds",
+  ]);
+  if (pausedTabIds.length === 0) return;
+
+  const targetTabs = await resolveTargetTabs(sender?.tab?.id);
+  const targetIdSet = new Set(targetTabs.map((t) => t.id));
+
+  for (const tabId of pausedTabIds) {
+    if (!targetIdSet.has(tabId)) continue;
+    try {
+      await chrome.scripting.executeScript({
+        target: { tabId },
+        func: playAllMediaInPage,
+      });
+    } catch (e) {
+      /* 탭이 닫혔거나 권한이 없는 페이지 등은 조용히 무시 */
+    }
+  }
+
+  // 재생을 시도했으니(대상에서 빠져 더 이상 추적할 필요 없는 탭 포함) 추적 목록 비움.
+  // 리셋마다 반복 play() 되어도 이미 재생 중인 미디어의 play()는 사실상 no-op이라
+  // 문제 없음(요구사항 3-1의 검증 대상).
+  await chrome.storage.session.set({ pausedTabIds: [] });
+}
--- a/manifest.json
+++ b/manifest.json
@@ -1,8 +1,8 @@
 {
   "manifest_version": 3,
-  "name": "메이플 타이머 - 미디어 자동 일시정지",
-  "version": "1.0",
-  "description": "MesoTimer 알람이 울리면 다른 탭에서 재생 중인 영상(유튜브, 넷플릭스 등)을 자동으로 일시정지합니다.",
+  "name": "메이플 타이머 - 미디어 자동 정지/재생",
+  "version": "2.0",
+  "description": "MesoTimer 알람이 울리면 다른 탭(또는 선택한 탭)의 영상을 일시정지하고, 타이머가 리셋되면 다시 재생합니다.",
   "permissions": ["tabs", "scripting", "storage"],
   "host_permissions": ["<all_urls>"],
   "background": {
@@ -18,6 +18,6 @@
   ],
   "action": {
     "default_popup": "popup.html",
-    "default_title": "메이플 타이머 미디어 자동 정지"
+    "default_title": "메이플 타이머 미디어 자동 정지/재생"
   }
 }
--- a/popup.html
+++ b/popup.html
@@ -4,7 +4,9 @@
 <meta charset="UTF-8">
 <style>
   body {
-    width: 220px;
+    width: 320px;
+    max-height: 480px;
+    overflow-y: auto;
     font-family: -apple-system, "Malgun Gothic", sans-serif;
     padding: 14px;
     font-size: 13px;
@@ -14,7 +16,12 @@
     font-size: 13px;
     margin: 0 0 10px;
   }
-  label {
+  h2 {
+    font-size: 12px;
+    margin: 16px 0 6px;
+    color: #52525b;
+  }
+  label.toggle-row {
     display: flex;
     align-items: center;
     gap: 8px;
@@ -25,16 +32,99 @@
     color: #71717a;
     font-size: 12px;
   }
+  hr {
+    border: none;
+    border-top: 1px solid #e4e4e7;
+    margin: 14px 0 0;
+  }
+  .hint {
+    color: #a1a1aa;
+    font-size: 11px;
+    line-height: 1.5;
+    margin: 2px 0 10px;
+  }
+  .tab-row {
+    display: flex;
+    align-items: center;
+    gap: 8px;
+    padding: 6px 0;
+    border-bottom: 1px solid #f4f4f5;
+  }
+  .tab-row img {
+    width: 16px;
+    height: 16px;
+    flex-shrink: 0;
+    border-radius: 3px;
+  }
+  .tab-info {
+    flex: 1;
+    min-width: 0;
+  }
+  .tab-title {
+    font-size: 12px;
+    white-space: nowrap;
+    overflow: hidden;
+    text-overflow: ellipsis;
+  }
+  .tab-domain {
+    font-size: 10px;
+    color: #a1a1aa;
+  }
+  .btn {
+    font-size: 11px;
+    padding: 4px 8px;
+    border-radius: 6px;
+    border: 1px solid #d4d4d8;
+    background: #fafafa;
+    cursor: pointer;
+    color: #3f3f46;
+    flex-shrink: 0;
+    white-space: nowrap;
+  }
+  .btn:hover { background: #f0f0f1; }
+  .btn.remove {
+    color: #ef4444;
+    border-color: #fecaca;
+  }
+  .badge {
+    font-size: 10px;
+    padding: 2px 6px;
+    border-radius: 999px;
+    background: #ecfdf5;
+    color: #10b981;
+    flex-shrink: 0;
+  }
+  .badge.title { background: #eff6ff; color: #3b82f6; }
+  .empty {
+    color: #a1a1aa;
+    font-size: 11px;
+    padding: 4px 0;
+  }
 </style>
 </head>
 <body>
-  <h1>메이플 타이머 미디어 자동 정지</h1>
-  <label>
+  <h1>메이플 타이머 미디어 자동 정지/재생</h1>
+  <label class="toggle-row">
     <input type="checkbox" id="enabledCheck">
-    알람 울리면 다른 탭 영상 일시정지
+    알람/리셋 시 영상 자동 정지·재생
   </label>
   <div id="status"></div>
 
+  <hr>
+  <h2>제어 대상 탭 (선택됨)</h2>
+  <div class="hint">
+    아무 탭도 선택하지 않으면 열려 있는 모든 탭이 대상입니다.
+    특정 탭만 제어하고 싶을 때만 아래에서 골라 추가하세요.
+  </div>
+  <div id="selectedList"></div>
+
+  <h2>탭 추가</h2>
+  <div class="hint">
+    에피소드마다 제목이 바뀌는 사이트(애니메이션 등)는 <b>도메인</b>,
+    한 사이트 안에 여러 탭이 열리는 유튜브 같은 경우는 <b>제목</b>으로 추가하세요.
+  </div>
+  <div id="candidateList"></div>
+
   <script src="popup.js"></script>
 </body>
 </html>
--- a/popup.js
+++ b/popup.js
@@ -1,17 +1,159 @@
 const check = document.getElementById("enabledCheck");
 const status = document.getElementById("status");
+const selectedListEl = document.getElementById("selectedList");
+const candidateListEl = document.getElementById("candidateList");
+
+// ---- 켜짐/꺼짐 토글 (기존 기능) ----------------------------------------
 
 chrome.storage.local.get(["enabled"], ({ enabled }) => {
   check.checked = enabled !== false; // 기본값 true
-  render();
+  renderStatus();
 });
 
 check.addEventListener("change", () => {
-  chrome.storage.local.set({ enabled: check.checked }, render);
+  chrome.storage.local.set({ enabled: check.checked }, renderStatus);
 });
 
-function render() {
+function renderStatus() {
   status.textContent = check.checked
-    ? "켜짐: 타이머 알람이 울리면 다른 탭 영상이 자동으로 멈춥니다."
-    : "꺼짐: 자동 정지 기능이 비활성화되었습니다.";
+    ? "켜짐: 알람이 울리면 선택한 탭(또는 전체 탭)의 영상이 자동으로 멈추고, 리셋되면 다시 재생됩니다."
+    : "꺼짐: 자동 정지/재생 기능이 비활성화되었습니다.";
+}
+
+// ---- 탭 선택/매칭 UI ----------------------------------------------------
+
+function getDomain(url) {
+  try {
+    return new URL(url).hostname;
+  } catch (e) {
+    return "";
+  }
 }
+
+function isControllableTab(tab) {
+  return !!tab.id && /^https?:/.test(tab.url || "");
+}
+
+function escapeHtml(str) {
+  const div = document.createElement("div");
+  div.textContent = str ?? "";
+  return div.innerHTML;
+}
+
+async function getSelectedTargets() {
+  const { selectedTargets } = await chrome.storage.session.get([
+    "selectedTargets",
+  ]);
+  return selectedTargets || [];
+}
+
+async function setSelectedTargets(targets) {
+  await chrome.storage.session.set({ selectedTargets: targets });
+}
+
+async function addTarget(tab, matchBy) {
+  const targets = await getSelectedTargets();
+  targets.push({
+    tabId: tab.id,
+    domain: getDomain(tab.url),
+    titleAtSelection: tab.title || "",
+    matchBy, // "domain" | "title"
+  });
+  await setSelectedTargets(targets);
+  await renderAll();
+}
+
+async function removeTarget(index) {
+  const targets = await getSelectedTargets();
+  targets.splice(index, 1);
+  await setSelectedTargets(targets);
+  await renderAll();
+}
+
+async function renderAll() {
+  const [tabs, targets] = await Promise.all([
+    chrome.tabs.query({}),
+    getSelectedTargets(),
+  ]);
+  const controllableTabs = tabs.filter(isControllableTab);
+  const selectedTabIds = new Set(targets.map((t) => t.tabId));
+
+  // 선택된 대상 목록
+  if (targets.length === 0) {
+    selectedListEl.innerHTML = `<div class="empty">선택된 탭 없음 (전체 탭 대상)</div>`;
+  } else {
+    selectedListEl.innerHTML = targets
+      .map((t, i) => {
+        const badgeClass = t.matchBy === "title" ? "badge title" : "badge";
+        const badgeText = t.matchBy === "title" ? "제목" : "도메인";
+        const label =
+          t.matchBy === "title" ? t.titleAtSelection : t.domain;
+        return `
+          <div class="tab-row">
+            <div class="tab-info">
+              <div class="tab-title" title="${escapeHtml(
+                t.titleAtSelection
+              )}">${escapeHtml(label || "(알 수 없음)")}</div>
+              <div class="tab-domain">${escapeHtml(t.domain)}</div>
+            </div>
+            <span class="${badgeClass}">${badgeText}</span>
+            <button class="btn remove" data-remove-index="${i}">삭제</button>
+          </div>`;
+      })
+      .join("");
+
+    selectedListEl.querySelectorAll("[data-remove-index]").forEach((btn) => {
+      btn.addEventListener("click", () => {
+        removeTarget(Number(btn.dataset.removeIndex));
+      });
+    });
+  }
+
+  // 추가 가능한(아직 선택 안 된) 탭 목록
+  const candidates = controllableTabs.filter(
+    (t) => !selectedTabIds.has(t.id)
+  );
+
+  if (candidates.length === 0) {
+    candidateListEl.innerHTML = `<div class="empty">추가할 수 있는 탭이 없습니다.</div>`;
+  } else {
+    candidateListEl.innerHTML = candidates
+      .map((t) => {
+        const favicon = t.favIconUrl || "";
+        return `
+          <div class="tab-row">
+            ${
+              favicon
+                ? `<img src="${escapeHtml(favicon)}" alt="">`
+                : ""
+            }
+            <div class="tab-info">
+              <div class="tab-title" title="${escapeHtml(t.title || "")}">${escapeHtml(
+          t.title || "(제목 없음)"
+        )}</div>
+              <div class="tab-domain">${escapeHtml(getDomain(t.url))}</div>
+            </div>
+            <button class="btn" data-add-domain="${t.id}">도메인</button>
+            <button class="btn" data-add-title="${t.id}">제목</button>
+          </div>`;
+      })
+      .join("");
+
+    const tabById = new Map(candidates.map((t) => [t.id, t]));
+
+    candidateListEl.querySelectorAll("[data-add-domain]").forEach((btn) => {
+      btn.addEventListener("click", () => {
+        const tab = tabById.get(Number(btn.dataset.addDomain));
+        if (tab) addTarget(tab, "domain");
+      });
+    });
+    candidateListEl.querySelectorAll("[data-add-title]").forEach((btn) => {
+      btn.addEventListener("click", () => {
+        const tab = tabById.get(Number(btn.dataset.addTitle));
+        if (tab) addTarget(tab, "title");
+      });
+    });
+  }
+}
+
+renderAll();
--- a/README.md
+++ b/README.md
@@ -1,17 +1,24 @@
-# 메이플 타이머 - 미디어 자동 일시정지 (크롬 확장 프로그램)
+# 메이플 타이머 - 미디어 자동 정지/재생 (크롬 확장 프로그램)
 
-MesoTimer(타이머 HTML 페이지)의 알람이 울리는 순간, 크롬의 **다른 탭**에서 재생 중인
-영상(유튜브, 넷플릭스, 트위치 등 `<video>`/`<audio>` 태그를 쓰는 모든 사이트)을
-자동으로 일시정지합니다.
+MesoTimer(타이머 HTML 페이지)의 **알람이 울리는 순간** 다른 탭(또는 사용자가 지정한 탭)에서
+재생 중인 영상(유튜브, 넷플릭스, 트위치 등 `<video>`/`<audio>` 태그를 쓰는 모든 사이트)을
+자동으로 **일시정지**하고, 타이머가 **리셋되는 순간**(반복 알림 후 자동 재시작, 마우스 휠 보정,
+더블클릭 리셋 등) 그 탭들을 다시 **재생**시켜 줍니다.
 
 브라우저 정책상 웹페이지(HTML)만으로는 다른 탭을 제어할 수 없기 때문에,
 이 기능은 반드시 크롬 확장 프로그램 형태로 설치해야 동작합니다.
 
-> **⚠️ v2 업데이트: 타이머 HTML도 함께 교체해야 합니다.**
-> PIP(화면 속 화면) 모드에서 알람 감지가 안 되던 문제와, PIP 상태에서 카운트 방향이
-> 실수로 뒤집히던 문제를 고치기 위해 `index.html`(타이머 페이지)도 함께 수정했습니다.
-> 기존에 쓰시던 타이머 HTML을 이 폴더의 `index.html`로 교체(또는 깃허브 페이지에 재배포)
-> 해주셔야 아래 "PIP 모드 관련 수정 사항"이 적용됩니다. 자세한 내용은 하단 참고.
+크롬뿐 아니라 엣지, 웨일 등 크로미움 기반 브라우저에서는 폴더 그대로 로드하면
+동일하게 동작합니다(별도 분기 처리 필요 없음).
+
+> **⚠️ 2차 개선 업데이트: `index.html`과 확장 프로그램 파일을 모두 교체해야 합니다.**
+> 이번 개선으로 "리셋 시 자동 재생" 기능과 "제어할 탭 직접 선택" 기능이 추가됐습니다.
+> - `index.html`(타이머 페이지)이 리셋 시점마다 신호를 보내도록 수정됐으니, 깃허브
+>   페이지에 올려둔 타이머 HTML을 이 폴더의 `index.html`로 교체·재배포해주세요.
+> - `background.js`, `watcher.js`, `popup.html`, `popup.js`, `manifest.json`도 모두
+>   바뀌었으니, 확장 프로그램 폴더 전체를 이 버전으로 교체한 뒤
+>   `chrome://extensions`에서 확장 프로그램을 새로고침(리로드)해주세요.
+> 자세한 변경 내용은 하단 "작업 기록" 섹션을 참고하세요.
 
 ---
 
@@ -24,7 +31,7 @@
 3. 화면 오른쪽 위에 있는 **"개발자 모드"** 토글을 켭니다.
 4. 왼쪽 위 **"압축해제된 확장 프로그램을 로드합니다"** 버튼을 클릭합니다.
 5. 방금 저장한 `maple-timer-auto-pause` 폴더를 선택합니다.
-6. 목록에 "메이플 타이머 - 미디어 자동 일시정지"가 뜨면 설치 완료입니다.
+6. 목록에 "메이플 타이머 - 미디어 자동 정지/재생"이 뜨면 설치 완료입니다.
 
 설치 후에는 크롬 툴바 오른쪽 위 퍼즐 아이콘(확장 프로그램)을 눌러
 이 확장 프로그램을 고정해두면 켜짐/꺼짐 상태를 바로 확인할 수 있습니다.
@@ -34,25 +41,46 @@
 ## 사용 방법
 
 1. 평소처럼 타이머 HTML 페이지를 한 탭에서 켜두고, 다른 탭에서 유튜브/넷플릭스 등을 재생합니다.
-2. 타이머 알람이 울리는 순간(화면이 빨갛게 경고 상태로 바뀌는 순간), 다른 탭의 영상이
+2. 타이머 알람이 울리는 순간(화면이 빨갛게 경고 상태로 바뀌는 순간), 대상 탭의 영상이
    자동으로 일시정지됩니다.
-3. 재생은 자동으로 다시 시작되지 않습니다. 하시던 설치를 마치고 직접 재생 버튼을
-   눌러 이어보시면 됩니다. (자동 재생까지 하면 오히려 원치 않는 타이밍에 영상이
-   켜질 수 있어서, 일부러 "정지"만 하도록 만들었습니다.)
+3. 타이머가 리셋되는 순간(반복 알림 지속시간이 끝나고 자동으로 다시 시작될 때, 마우스 휠로
+   보정할 때, 더블클릭으로 초기화할 때) **확장 프로그램이 직접 정지시켰던 영상만** 자동으로
+   다시 재생됩니다. 사용자가 스스로 일시정지한 영상은 건드리지 않습니다.
 
 끄고 싶을 때는 확장 프로그램 아이콘을 눌러 체크박스를 해제하면 됩니다.
 
+### 특정 탭만 제어하고 싶을 때 (선택 사항)
+
+기본값은 **열려 있는 모든 탭**이 대상입니다. 유튜브에서 노래를 들으면서 동시에 다른
+탭에서 방송을 보는 등, 미디어 탭이 여러 개라 특정 탭만 제어하고 싶다면:
+
+1. 확장 프로그램 아이콘을 눌러 팝업을 엽니다.
+2. "탭 추가" 목록에서 원하는 탭 옆의 **도메인** 또는 **제목** 버튼을 누릅니다.
+   - **도메인**: 애니메이션/드라마처럼 에피소드마다 탭 제목이 바뀌는 사이트에 적합합니다.
+   - **제목**: 유튜브처럼 같은 사이트 안에 여러 용도의 탭(방송 탭 + 음악 탭 등)이
+     동시에 열릴 수 있는 경우에 적합합니다. (재생 중인 곡이 바뀌면 탭 제목도 바뀌므로
+     재연결이 끊길 수 있지만, 보통 사냥 시간이 곡 하나보다 훨씬 길어 큰 문제가 되지 않습니다.)
+3. 선택한 탭은 목록에 표시되며, 삭제 버튼으로 언제든 뺄 수 있습니다.
+4. **선택된 탭을 모두 지우면(아무것도 선택 안 하면) 다시 기본값인 "전체 탭 대상"으로
+   돌아갑니다.**
+
+선택 정보는 `chrome.storage.session`에 저장되어 **브라우저(크롬)를 완전히 종료하기 전까지만
+유지**되고, 브라우저를 껐다 켜면 초기화됩니다.
+
 ---
 
 ## 동작 원리 (참고)
 
 - 타이머 페이지에 `#timerDisplay` 요소가 있는지 확인해서, "이 페이지가 MesoTimer인지"
   자동으로 판별합니다. (다른 사이트에서는 아무 동작도 하지 않습니다.)
-- 타이머 알람이 울리면 `<body>` 태그에 `alarm-state` 클래스가 추가되는 것을
-  감지해서, 그 순간 확장 프로그램에 신호를 보냅니다.
-- 신호를 받으면 열려 있는 모든 탭(타이머 탭 제외)에 스크립트를 넣어
-  재생 중인 `<video>`, `<audio>` 요소를 찾아 `pause()`만 호출합니다.
-  영상을 끄거나, 페이지를 닫거나, 다른 조작을 하지 않습니다.
+- **정지**: 타이머 알람이 울리면 `<body>` 태그에 `alarm-state` 클래스가 추가되는 것을
+  감지해서 확장 프로그램에 신호를 보내고, 대상 탭에서 재생 중인 `<video>`, `<audio>`를
+  찾아 `pause()`만 호출합니다. 이때 정지시킨 탭 목록을 기억해둡니다.
+- **재생**: 타이머가 리셋될 때마다(자동 loop 재시작, 휠 보정, 더블클릭 등) `<body>` 태그의
+  `data-reset-seq` 값이 바뀌는 것을 감지해서 확장 프로그램에 신호를 보내고, **방금 위에서
+  기억해둔("확장 프로그램이 직접 정지시킨") 탭에만** `play()`를 호출합니다. 사용자가 직접
+  멈춘 영상이나, 애초에 정지시킨 적 없는 탭은 건드리지 않습니다.
+- 영상을 끄거나, 페이지를 닫거나, 그 외 다른 조작은 하지 않습니다.
 
 ---
 
@@ -100,8 +128,9 @@
 
 ## 권한 안내
 
-- `tabs`, `scripting`: 다른 탭에 "일시정지" 명령만 전달하기 위해 필요합니다.
-- `storage`: 켜짐/꺼짐 설정을 저장하기 위해 필요합니다.
+- `tabs`, `scripting`: 대상 탭에 "일시정지"/"재생" 명령만 전달하기 위해 필요합니다.
+- `storage`: 켜짐/꺼짐 설정(`storage.local`)과 선택한 탭 목록·정지시킨 탭 추적
+  (`storage.session`, 브라우저를 끄기 전까지만 유지)을 저장하기 위해 필요합니다.
 - `<all_urls>`: 어떤 사이트에서 영상을 보고 있어도 동작하게 하기 위한 권한입니다.
   이 확장 프로그램은 영상 재생/정지 외에 어떤 데이터도 읽거나 전송하지 않습니다.
 
@@ -111,3 +140,18 @@
   방식이라, 크롬 업데이트 시 가끔 "개발자 모드 확장 프로그램" 경고 배너가 보일 수
   있습니다. 정상 동작에는 문제 없습니다.
 - 폴더를 이동/삭제하면 확장 프로그램도 함께 사라지니 주의하세요.
+
+---
+
+## 작업 기록 (Changelog)
+
+> **규칙: 앞으로 코드에 변경이 생길 때마다(작은 수정이라도) 이 표에 한 줄씩 추가합니다.**
+> 형식: `날짜 | 무엇을 바꿨는지(요약) | 바뀐 파일`
+> 다음 세션에서 이전 작업 맥락을 빠르게 파악하기 위한 용도이니, 요구사항 정리만 한
+> 세션이라도(코드 변경 없음) 한 줄 남겨두면 좋습니다.
+
+| 날짜 | 내용 | 바뀐 파일 |
+|---|---|---|
+| (최초 배포) | 정지 전용 버전: 타이머 알람(0초) 시 열려있는 모든 탭의 영상/오디오를 일시정지 | `background.js`, `watcher.js`, `manifest.json`, `popup.html`, `popup.js` |
+| 1차 개선 (PIP-FIX) | PIP(화면 속 화면) 모드에서 알람 감지가 안 되던 문제, 카운트 방향이 실수로 뒤집히던 문제 수정. 확장 프로그램 코드는 변경 없음 | `index.html` |
+| 2차 개선 (이번 작업) | ① 타이머 **리셋**(자동 loop 재시작 / 휠 보정 / 더블클릭)마다 재생 신호를 보내, 확장 프로그램이 직접 정지시킨 탭만 다시 자동 재생하도록 추가. ② 정지/재생 대상을 "선택한 특정 탭"으로 좁힐 수 있는 팝업 UI 추가(도메인/제목 매칭 방식을 탭마다 직접 선택, 아무것도 선택 안 하면 기존처럼 전체 탭 대상) | `index.html`, `watcher.js`, `background.js`, `popup.html`, `popup.js`, `manifest.json` |
