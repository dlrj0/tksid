# 메이플 타이머 — 다음 작업 아이디어 정리 (2026-07-27, 최종)

> 이 문서는 `단축키.md`를 대체합니다. 완료된 설계 내용은 요약만 남기고,
> 앞으로 진행할 작업 방향을 이 파일 하나로 관리합니다. 작업을 진행하면서
> 결정이 바뀌거나 구현이 끝나면 이 문서도 함께 갱신해주세요.

---

## 0. 작업 강도 안내

이번 정리는 "중간" 강도로 진행했습니다. 다음 세션에서 실제 코드 구현을
시작할 때도 "중간"을 유지하는 걸 권장합니다 — 특히 프리셋 기능처럼 사이트
쪽 로직이 복잡한 항목은 중간 이상이 필요합니다.

## 0-1. 공통 원칙 (기존 그대로 유지)

- 메이플 정책 위반 소지가 있는 기능(게임에 입력 주입 등)은 절대 하지 않음.
- 기존 기능은 항상 유지. 수정이 필요하면 "대체"가 아니라 "개선" 방향으로.
- 새 기능 추가 시 불필요한 중복 호출, 리스너 충돌 여부를 항상 점검.

---

## 1. 완료 항목 중 계속 참고할 것 (`단축키.md`에서 이어옴)

- **창(포그라운드) 필터링은 순수 확장프로그램으로 불가능** — OS가 주는 정보라
  Chrome 확장 API로는 읽을 수 없음. Native Messaging 헬퍼 프로그램을 붙이면
  가능하지만 별도 설치가 필요해 기각됨. → 대신 "다중 단축키 + 마스터 스위치"
  구조로 대체 완료.
- **전역 단축키(`global: true`)는 OS 레벨에서 키 입력을 통째로 가로챈다.**
  껐다 켜도(마스터 스위치 off) 가로채기 자체는 풀리지 않고 "반응만 안 할 뿐"
  키는 계속 이 확장프로그램이 삼킴. 아래 3번 항목(가로채기 확인)과 직결되는
  배경 지식이라 계속 유지.
- 팝업에서 **실제 키 캡처 UI는 만들 수 없음** — `chrome.commands`는 JS로 키를
  `set()`하는 API 자체가 없고, 배정은 `chrome://extensions/shortcuts`에서만
  가능함. 아래 6번(핸들 정렬) 작업 시에도 이 제약은 그대로 적용됨.
- 확장프로그램 하나가 `suggested_key`로 자동 지정 가능한 단축키는 최대 4개
  (현재 4개를 이미 다 씀). 5번째부터는 `suggested_key` 없이 정의만 해두고
  사용자가 수동 배정.

---

## 2. 단축키 사용 피드백 개선

현재는 피크음(주파수로 구분)만 있어서, "지금 어떤 단축키가 눌렸는지"를 소리
톤만으로 구분해야 함 → 헷갈릴 수 있음.

- **제안**: 타이머 페이지에 짧은(1~2초) 인페이지 토스트를 추가해서, 단축키가
  눌릴 때마다 "타이머 리셋됨", "일시정지" 같은 텍스트를 화면 우상단 등에
  잠깐 띄우는 방식.
  - 단, 메이플 게임 화면을 보고 있을 때는 브라우저 탭이 안 보이므로 토스트만으로는
    부족함. **피크음(청각) + 토스트(시각, 브라우저 확인 시)를 병행**하는 걸 권장.
- 이 "피드백"이 아래 3번(가로채기 자체가 됐는지 여부 확인)과는 다른 문제라는
  점을 구분해두면 좋음 — 2번은 "어떤 기능이 실행됐는지", 3번은 "이 키 입력이
  확장프로그램한테 도달하긴 했는지"임.

## 3. 단축키 가로채기(등록) 여부를 사용자가 확인할 수 있는 기능

- **클로드 의견**: "특정 키 조합이 실제로 이 확장프로그램에 가로채기 됐는지"는
  JS 레벨에서 직접 알 방법이 없음(1번 항목 참고). 다만 아래 간접적인 방법으로
  실용적인 확인은 가능:
  1. `chrome.commands.getAll()`로 지금 배정된 키 조합 자체를 보여주는 건
     이미 팝업에 구현돼 있음(단축키 목록).
  2. 각 단축키가 실제로 `chrome.commands.onCommand`에서 발동될 때마다
     `chrome.storage.local`에 "마지막 발동 시각"을 기록해두고, 팝업의
     단축키 목록에 "마지막 사용: 3분 전" 같은 문구를 붙이는 방식.
  3. 안 잡히는 경우(키를 눌러도 시각이 갱신 안 됨)의 흔한 원인은 대부분
     "다른 프로그램이 먼저 그 키를 가로챔"이므로, 팝업에 이 안내문을 함께
     넣어주면 사용자가 다음에 뭘 해야 할지 알 수 있음.

## 4. 동영상 탐색(되감기/넘기기) 단축키

- 요청: 되감기/넘기기, 초 단위(1~15초)는 설정에서 조절 가능하도록.
- 구조적으로는 기존 `force-pause-media`/`force-resume-media`와 같은 패턴으로
  구현 가능 (`chrome.scripting.executeScript`로 대상 탭의 `<video>.currentTime`을
  조정). `resolveTargetTabs()` 재사용 가능.
- 다만 **2개의 새 커맨드(뒤로/앞으로)가 필요**하고 이미 `suggested_key` 4개
  슬롯을 다 썼으므로 기본 키 배정 없이 추가 → 사용자가 직접 배정.
- 설정값(초 단위)은 `storage.local`에 숫자 하나만 추가하면 되는 간단한 구조.
- **주의**: 유튜브 등 사이트에 따라 `<video>` 엘리먼트가 여러 개(광고 포함)
  존재할 수 있어 "재생 중인" 비디오만 골라 조정하는 필터가 필요함(기존
  pause/resume 로직에 이미 있을 가능성이 높음 — 확인 후 재사용).
- `<video>` 표준 속성만 조작하는 기능이라 **모든 사이트에 보편적으로
  동작**함 — 사이트별 추가 작업 불필요.

## 5. OTT 다음화/이전화 재생 — 우선순위 후순위

- 되감기/넘기기와 달리, 이건 각 사이트가 자기 페이지에 만들어 둔 버튼을
  대신 클릭해주는 방식이라 **사이트마다 선택자(selector)가 다르고 UI
  개편 시 깨질 수 있음.**
- (조사 완료) 유튜브도 "보편적 검증" 기준으로 적합하지 않음 — 유튜브의
  다음 영상 버튼(`.ytp-next-button`)은 **재생목록(playlist) 안에서 재생
  중일 때만 나타나고, 단일 영상만 볼 때는 버튼 자체가 없거나 숨겨진
  경우가 흔함.** 즉 유튜브도 상황에 따라 있다/없다가 갈리는 조건부
  케이스라 "유튜브로 먼저 검증 후 확장"이 성립하지 않음.
- 기술적으로 불가능한 건 아니고(이미 화면에 보이는 버튼을 클릭하는 것뿐이라
  정책 문제도 없음), 단지 "한 번 만들면 어디서든 되는" 기능이 아니라서
  순서상 뒤로 미룸. 나중에 실제로 필요한 사이트를 특정해서 그 사이트만
  조사 후 구현하는 걸 권장.

## 6. 팝업 — 단축키 목록 드래그 정렬(핸들)

- 요청: "사용 가능한 단축키 목록" 펼쳤을 때 핸들로 순서 변경, 단축키가
  실제 배정된 것만 위/아래로 몰아보기.
- 구현 난이도는 낮음: 순서는 화면 표시용 정렬일 뿐이라 실제 `chrome.commands`
  등록과는 무관 — `storage.local`에 "표시 순서" 배열만 저장하면 됨(HTML5
  `draggable` 속성 + 순서 배열 재정렬, 외부 라이브러리 불필요).
- "배정된 것만 자동으로 위로" 정렬 옵션은 별도 토글로 추가하면 수동 정렬과
  충돌 없이 공존 가능(정렬 기준을 "사용자 지정" vs "배정 여부 우선"으로 나눔).

## 7. 사이트 실행 단축키 (여러 탭 동시 실행 + 시크릿 모드)

- 요청: 등록한 사이트 여러 개(예: 타이머 사이트 + OTT 사이트)를 단축키
  하나로 한꺼번에 새 탭으로 열기, 시크릿 모드 선택 가능.
- 구현 방향: 새 커맨드 1개(`open-preset-sites`) + 팝업에 URL 목록 관리 UI.
  `chrome.tabs.create({ url, windowId })`로 순차 생성.
- **시크릿 모드는 주의가 필요함**: `chrome.windows.create({ incognito: true })`로
  시크릿 창을 여는 것은 가능하지만, **사용자가 확장프로그램 관리 화면에서
  "시크릿 모드에서 허용"을 직접 켜줘야만** 확장프로그램이 시크릿 컨텍스트에
  접근/실행할 수 있음(코드로 우회 불가, Chrome 정책). 이 부분은 안내문으로
  명확히 알려줘야 함.

---

## 8. 프리셋 기능 (사이트 쪽 대규모 기능 — 별도 세션에서 진행 권장)

아래는 사용자가 정리한 원문을 그대로 남겨둡니다. **다음 세션에서 "중간"
강도로 따로 설계 문서를 만들어 진행하는 걸 권장**합니다.

> 타이머 시간이나 버튼 활성화등 세팅 한번으로 타이머 사이트 재접속시 같은 값이
> 세팅되도록 확장프로그램 폴더에 파일으로 기본 프리셋을 수정/저장, 기본 프리셋은
> 세팅 변경시 항상 파일 수정되어 저장되도록.
> 확장프로그램이 없는 사람에게는 기존과 동일하게 동작하도록 프리셋 기능 유지.
> 기존의 1~4개의 프리셋 기능도 확장프로그램이 있는 사람에게는 확장프로그램
> 폴더의 프리셋 파일에 저장되어 수정되고, 사이트 재접속시에도 같은 값이 저장되어
> 불러올수 있도록 구현.
> 결국 기본,1~4 프리셋이 확장프로그램이 있는 사람에게는 확장프로그램폴더에
> 프리셋 파일에 세팅이 저장되고, 타이머 메인 화면에서 상단 버튼과 마찬가지로
> 마우스 포커스가없을때 사라지도록 현재 프리셋을 상단 중앙에 나타내 현재 어떤
> 프리셋적용중인지 나타내거나 프리셋 기능을 설정에서 메인화면으로 이동, 투명화는
> 메인화면의 LOOT와 메뉴, 다운버튼 등과같이 마우스 포커스가 없을때 사라져야 함.
> 지금은 프리셋 저장, 불러오기, 변경시 확인,취소를 나타내는 안내 팝업이 없어서
> 기본프리셋을 1~4번에 저장된 세팅을 무시하고 실수로 덮어씌워버릴 수 있으니
> 확인팝업 생성되도록 사이트의 프리셋 기능을 개선.
> 타이머 사이트 실행시 무조건 기본 프리셋의 기본 세팅값이 실행되고 값 변경시
> 기본 프리셋이 바로바로 수정이 되고, 프리셋 1~4번은 따로 저장,불러오기를 통해
> 값을 불러오는 거야. 하지만 타이머 사이트를 두개이상 사용하는 사람이 있을 수
> 있으니 설정에 "기본 프리셋 수정하기"와 같은 항목을 추가하고 항상 사이트 실행시
> 기본 프리셋이 설정되면서 이 설정 해제해서 타이머 사이트를 두개이상 사용할 수
> 있도록 해줘. 이 설정이 활성화되어있다면 기본 프리셋 설정이 수정되지만 이걸
> 해제하면 기본 프리셋이 설정이 불필요하게 바뀌지 않고 타이머 여러개 사용할때
> 좋을 것 같아.
> 프리셋이 기본일땐 수정되는게 바로바로 저장되어 반영이 되지만, 1~4번 프리셋을
> 사용중일때는 저장을 해야지만 세팅이 반영되도록 해줘. 프리셋1번에서 수정을
> 하면 1번에 바로저장되지않고, 저장을 다시해야 저장되도록 말이야.

**클로드가 미리 짚어두는 기술적 제약 (설계 시 반영 필요)**:
- "확장프로그램 폴더에 파일로 저장"은 웹페이지가 직접 파일 시스템에 쓸 수
  없으므로, 실제로는 **확장프로그램의 `chrome.storage.local`**에 저장하고,
  페이지는 `chrome.runtime.sendMessage`로 background에 저장을 요청하는
  구조가 될 것으로 예상됨. 사용자가 원하는 결과("재접속해도 유지, 확장프로그램
  있는 사람만 저장")는 동일하게 달성됨. 다음 세션에서 재확인 필요.
- 확장프로그램이 없는 사람은 기존처럼 `localStorage`만 쓰는 경로를 그대로
  유지해야 하므로, 페이지 쪽에 "확장프로그램 감지" 분기가 필요함(이미
  `watcher.js`가 심어지는지로 감지 가능 — 기존 감지 패턴 재사용 가능).

---

## 9. 동영상 재생/로드 안정성 (강제 정지·버퍼링 영향 없어야 함)

- 현재 구현은 `<video>.pause()`/`play()`만 호출하고 탭 자체를 새로고침하거나
  네트워크 요청에 개입하지 않으므로, 구조적으로 버퍼링을 유발할 요인은 거의
  없다고 판단됨. 다만 실사용 중 특정 사이트에서 문제가 보고되면 사이트별로
  원인을 따로 조사해야 함.

## 10. 애니 사이트에서 스페이스바로 정지 후 단축키 제어가 안 되는 문제 — 우선순위 후순위

- 증상: 영상이 재생 중일 때는 단축키로 정지 잘 됨 → 영상을 스페이스바로 직접
  정지하면 그 이후로는 같은 영상에 대해 단축키 제어(재생 등)가 안 됨.
- **클로드 추정 원인**: 사이트 자체의 커스텀 플레이어가 스페이스바 입력을 받아
  자기 내부 상태를 따로 관리하는 경우가 많음. 확장프로그램이 `<video>.play()`를
  호출해도, 사이트 스크립트가 내부 상태와 실제 상태가 다르다고 판단해 다시
  멈추는 등의 충돌이 생길 수 있음. **사이트마다 다른 구조 문제**라 일반적으로
  고치기 어려움 — 대응하려면 사이트 특정 코드가 필요하고 신중하게 접근해야 함.
- **다음 단계 제안**: 문제가 발생하는 사이트를 특정해서 개발자도구로 상태값을
  먼저 확인한 뒤 판단. 다른 항목들 구현 경험이 쌓인 뒤 진행하면 더 수월할 것.

---

## 11. 정지된 탭 목록 — 개별 재생 버튼 (클로드 제안, 채택)

지금은 "지금 정지된 탭" 목록이 보여주기만 하고, 리셋이 와야 전체가 한 번에
재생됨. 목록의 탭 하나씩에 "지금 이것만 재생" 버튼을 붙이면, 여러 영상 중
하나만 먼저 보고 싶을 때 리셋을 기다리지 않고 개별로 풀어줄 수 있음.
`resumeTargetTabs()` 로직을 탭 하나 단위로 재사용하면 구현 난이도는 낮음.

## 12. 제어 대상 탭 — 제외 목록(블랙리스트) 모드 추가 (클로드 제안, 채택)

지금 "제어 대상 탭"은 선택하면 그것만, 선택 안 하면 전체가 대상인 방식(포함
방식)만 있음. 반대로 "이 탭들은 절대 건드리지 마" 방식(제외 방식)을 추가하면,
미디어 탭이 많이 열려 있을 때 "제어할 것만 골라 넣기"보다 "제외할 것만 몇 개
빼기"가 더 편한 경우에 대응됨. 저장 구조(`selectedTargets`)에 모드 플래그만
추가하면 기존 로직과 충돌 없이 공존 가능.

## 13. 알람 예고(사전 경고) 기능 (클로드 제안, 채택)

알람이 울리기 몇 초 전에 짧은 예비 신호(약한 피크음 1회 등)를 줘서, 정지되기
전에 미리 준비할 수 있게. 타이머 쪽에 남은 시간 값이 이미 있으니 "N초 전"
트리거 추가는 어렵지 않음.

## 14. 볼륨 단축키(무음/원상복구 토글) (클로드 제안, 채택)

정지 대신 "소리만 끄기"를 원하는 경우도 있을 수 있어서, 되감기/넘기기(4번)와
같은 패턴으로 볼륨 토글도 같이 만들면 재사용성이 좋음.

---

## 15. 메이플 정책 위반(제재) 가능성 최종 검토

전체 아이디어를 다시 훑어보고 결론 내리면: **여기서 다루는 기능들 중 메이플
게임 클라이언트에 직접 관여하는 기능은 하나도 없습니다.**

- 이 확장프로그램/사이트가 실제로 다루는 대상은 딱 세 가지뿐입니다:
  1. **크롬 확장프로그램 API**(단축키 감지, 탭 목록, storage) — 게임과 무관.
  2. **MesoTimer 웹페이지**(팬메이드 타이머 사이트, 게임 클라이언트 아님) —
     이 페이지의 타이머 값을 조작하는 것.
  3. **다른 브라우저 탭의 영상**(유튜브, OTT, 애니 사이트 등) — 게임과 무관.
- 게임 화면을 읽거나(스크린 캡처/OCR), 게임 프로세스에 입력을 주입하거나,
  게임 메모리에 접근하는 기능은 이번에 정리한 항목 중 **전혀 없습니다.**
  이번에 새로 추가된 아이디어(되감기/넘기기, 다음화/이전화, 볼륨 토글,
  제외 목록, 예고 알람 등)도 전부 위 세 가지 범위 안에서만 동작합니다.
- 유일하게 "매크로/핵" 판단이 필요했던 부분(전역 단축키로 리셋)은 이미
  1번 항목에 정리된 대로 **"감지만 함, 게임에 아무 것도 주입하지 않음"**
  구조로 확정돼 있고, 오늘 추가된 항목들도 이 원칙을 벗어나지 않습니다.
- **결론**: 지금까지 정리한 방향(4~14번 전부)은 게임 클라이언트와 아예
  접점이 없는, "브라우저 탭·타이머 웹페이지 자동화" 수준의 개인용 도구라서
  메이플 정책 위반 소지가 보이지 않습니다. 이대로 진행해도 될 것으로 판단합니다.
- **다만 참고**: 이건 클로드의 판단이고 넥슨의 100% 공식 확답은 아닙니다.
  개인적으로만 쓰는 지금 규모에서는 문제될 게 없어 보이지만, 만약 나중에
  이 확장프로그램을 다른 사람들과 공유/배포하는 규모로 커진다면, 그때는
  넥슨 고객센터에 한 번 문의해보는 걸 권장합니다(기존 결론 유지).

---

## 16. 작업 순서 제안 (우선순위)

**구현·테스트가 쉬운 것부터 먼저 진행**하고, 결과가 애매하거나 사이트마다
달라지는 항목(5번, 10번)은 뒤로 미뤄서 나중에 다시 판단하는 순서입니다.

1. 3번(마지막 사용 시각 표시) — 간단한 storage 추가
2. 6번(단축키 목록 드래그 정렬) — 팝업 UI만 수정
3. 2번(인페이지 토스트 피드백) — `타이머.html`에 소규모 추가
4. 4번(동영상 탐색: 되감기/넘기기) — 보편적으로 동작, 구현·테스트 명확
5. 14번(볼륨 단축키) — 4번과 같은 패턴 재사용, 같이 진행하면 효율적
6. 11번(정지된 탭 개별 재생 버튼) — 기존 로직 재사용, 난이도 낮음
7. 12번(제외 목록 모드) — 기존 저장 구조에 플래그만 추가
8. 13번(알람 예고 기능) — 우선순위는 낮지만 구현은 간단
9. 7번(사이트 실행 단축키) — 시크릿 모드 안내 포함, 중간 난이도
10. 8번(프리셋 기능) — 별도 세션, "중간" 강도로 설계부터 다시 시작 권장
11. **5번(OTT 다음화/이전화) — 후순위.** 위 항목들을 먼저 끝내고, 실제로
    필요한 사이트를 정해서 그때 다시 판단.
12. **10번(애니 사이트 스페이스바 이슈) — 후순위.** 다른 항목 구현 경험이
    쌓인 뒤 조사·판단.
